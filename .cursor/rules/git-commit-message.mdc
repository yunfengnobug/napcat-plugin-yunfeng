---
description: Git 提交说明须写清具体改动；功能变更同步 README；优先中文
alwaysApply: true
---

# Git 提交说明与文档同步

每次 `git commit` 的 message **禁止**只写笼统的一句话（如「修复问题」「更新代码」「调整配置」）。须让后人只看 message 就能知道改了什么、为什么改。

## 语言

- **正文、说明、列表**：尽量使用中文
- **type 前缀**：可使用英文（如 `feat` / `fix` / `chore` / `docs` / `refactor`）
- 推荐格式：`feat: 新增某某功能`

## 结构

1. **标题行**（必填）：`类型: 一句话概括主旨`
2. **空一行**
3. **正文**（必填，除非真是纯笔误级单行改动）：用若干短句或列表写清：
   - 改了哪些模块 / 行为
   - 关键约束或原因（若有）
   - 有意不改 / 兼容点（若有）

## 功能变更须同步 README

- 若本次改动涉及**功能增减、行为变化、配置项、使用方式**，须**同时更新** `README.md`（或项目既有文档）
- 文档与代码放在同一次提交，或紧随其后的 `docs:` 提交中，避免代码已变文档仍旧
- 纯内部重构、无用户可见变化时，可不改 README

## 禁止

- 只有标题、无正文，且标题本身含糊（「修复 bug」「优化」「更新部署」）
- 堆砌文件名而不说明行为变化
- 复制整段 diff 当说明

## 示例

```text
# ❌ 太笼统
fix: 优化匹配与首页。

# ✅ 足够具体
feat: 新增群消息关键词自动回复

- message-handler：支持按群配置关键词与回复内容
- WebUI 配置页增加关键词规则编辑
- README：补充命令说明与配置示例
```

多仓库同时提交时，**每个仓库各自**按本仓库改动写独立说明，不要共用一句空话。
