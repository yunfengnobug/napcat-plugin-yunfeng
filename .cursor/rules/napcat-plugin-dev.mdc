---
description: NapCat 插件开发规范：Apifox llms.txt、先报接口、功能按群开关与全局/批量、注释、默认 WebUI、避免过度解耦
alwaysApply: true
---

# NapCat 插件开发规范

## 1. 必须查阅官方文档（指南站 + Apifox llms.txt）

开发 / 修改插件前，**必须先查阅 NapCat 官方文档**，再据此实现。禁止凭记忆或猜测编造接口。

1. 使用/开发指南（安装、配置、插件机制、兼容表）：<https://napneko.github.io>
2. Action / 事件参数以 API 索引为准：<https://napcat.apifox.cn/llms.txt>，按索引打开对应 `.md`（如 `设置专属头衔` → `https://napcat.apifox.cn/226656931e0.md`）
3. 开发约束摘要见同目录 `napcat-official-dev-notes.mdc`；仅基于查阅到的文档编写代码

## 2. 开发功能前先报告所用接口

当用户要求**开发 / 新增 / 改造某个功能**时，在动手改代码之前必须先完成：

1. 通过 Apifox llms.txt 查阅相关接口（Action、事件、参数、返回值）
2. **先用中文回复用户**，明确列出本功能计划使用的接口，例如：
   - 接口名（如 `get_group_list`、`send_msg`）
   - 用途（为何需要它）
   - 关键参数 / 事件字段（若有）
3. 再开始实现；禁止跳过「先报接口」直接写代码

若查阅后发现缺少可用接口，先向用户说明，不要擅自用猜的 API 开工。

## 3. 每个功能必须写注释

插件中每个功能模块、导出函数、关键处理逻辑、配置项与 WebUI 交互点，都要有简洁中文注释，说明用途与关键行为。不要写显而易见的废话注释。

## 4. 默认提供 WebUI

除非用户**明确说明**不需要 WebUI，否则插件默认必须包含可用的 WebUI（配置页 / 管理界面），并与核心功能一并交付。

## 5. 功能开关：按群 + 全局 + 多选批量

新增或改造**面向群聊的功能点**时，必须支持可配置启停，而不是写死全局一直开：

1. **按群开关**：每个功能可在单个群内独立选择开启 / 关闭
2. **全局设置**：提供对该功能的全局默认（或「对所有群生效」的一键设置），未单独配置的群跟随全局
3. **多选批量**：WebUI 中用多选框勾选若干群后，可一次性对这些群写入同一套功能开关（开/关）
4. **生效前提**：仍须尊重现有「群授权」等门槛；未授权群不响应业务指令
5. **实现要求**：配置结构、权限判断与 WebUI（单群编辑 + 全局 + 批量）一并交付；处理消息前先查该群该功能是否开启

除非用户明确说某功能不需要按群开关，否则默认按本节实现。

## 6. 避免过度解耦，保证阅读体验

开发功能时**不要把函数拆得过碎**，以免跳转过多、主流程难读：

1. **同一业务主路径优先写在同一函数 / 邻近代码块**，让人能从上到下读懂「校验 → 处理 → 回复」
2. **仅在确有复用、或单段逻辑明显过长且有独立语义时再抽函数**；不要为「看起来更干净」而拆出只调用一次、名字泛化的小函数
3. **禁止无意义的中间层**：一层只调下一层、无额外逻辑的包装函数不要写
4. **跨文件拆分要克制**：一个功能的核心流程尽量留在同一文件；工具函数可以抽，但主叙事不要散落多处
5. 若必须拆分，抽出的函数命名要直接反映业务意图，并在调用处保持主流程仍可读
