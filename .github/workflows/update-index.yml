name: 自动更新插件索引

# 当 Release 发布时触发（插件作者 push tag → release.yml 构建发布 → 触发本工作流）
on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: '手动指定版本号（如 v1.0.0），留空则从最新 Release 获取'
        required: false
        default: ''

permissions:
  contents: write

env:
  INDEX_BRANCH: main
  INDEX_FILE: plugins.v4.json

jobs:
  update-index:
    name: 提交索引 PR（${{ matrix.index.label }}）
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        index:
          # 官方插件市场索引
          - repo: NapNeko/napcat-plugin-index
            name: napcat-plugin-index
            label: 官方索引
          # 社区插件索引
          - repo: HolyFoxTeam/napcat-plugin-community-index
            name: napcat-plugin-community-index
            label: 社区索引

    steps:
      - name: Checkout plugin repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Extract plugin metadata
        id: meta
        run: |
          # 从 package.json 读取插件元信息
          PLUGIN_ID=$(node -e "console.log(require('./package.json').name)")
          PLUGIN_NAME=$(node -e "console.log(require('./package.json').plugin || require('./package.json').name)")
          DESCRIPTION=$(node -e "console.log(require('./package.json').description || '')")
          AUTHOR=$(node -e "console.log(require('./package.json').author || '')")

          # 从 package.json 的 napcat 字段读取索引专用信息
          TAGS=$(node -e "console.log(JSON.stringify(require('./package.json').napcat?.tags || ['工具']))")
          MIN_VERSION=$(node -e "console.log(require('./package.json').napcat?.minVersion || '4.14.0')")
          HOMEPAGE=$(node -e "console.log(require('./package.json').napcat?.homepage || require('./package.json').homepage || '')")

          # 获取版本号
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ github.event.inputs.version }}" ]; then
            VERSION="${{ github.event.inputs.version }}"
          elif [ "${{ github.event_name }}" = "release" ]; then
            VERSION="${{ github.event.release.tag_name }}"
          else
            VERSION="v$(node -e "console.log(require('./package.json').version)")"
          fi
          CLEAN_VERSION=$(echo $VERSION | sed 's/^v//')

          # 构造下载链接
          REPO_FULL="${{ github.repository }}"
          DOWNLOAD_URL="https://github.com/${REPO_FULL}/releases/download/${VERSION}/${PLUGIN_ID}.zip"

          # 如果没有 homepage，使用仓库地址
          if [ -z "$HOMEPAGE" ]; then
            HOMEPAGE="https://github.com/${REPO_FULL}"
          fi

          echo "plugin_id=$PLUGIN_ID" >> $GITHUB_OUTPUT
          echo "plugin_name=$PLUGIN_NAME" >> $GITHUB_OUTPUT
          echo "version=$CLEAN_VERSION" >> $GITHUB_OUTPUT
          echo "description=$DESCRIPTION" >> $GITHUB_OUTPUT
          echo "author=$AUTHOR" >> $GITHUB_OUTPUT
          echo "homepage=$HOMEPAGE" >> $GITHUB_OUTPUT
          echo "download_url=$DOWNLOAD_URL" >> $GITHUB_OUTPUT
          echo "tags=$TAGS" >> $GITHUB_OUTPUT
          echo "min_version=$MIN_VERSION" >> $GITHUB_OUTPUT
          echo "tag_name=$VERSION" >> $GITHUB_OUTPUT

          echo "📦 Plugin: $PLUGIN_ID v$CLEAN_VERSION"
          echo "📥 Download: $DOWNLOAD_URL"
          echo "📚 目标索引: ${{ matrix.index.repo }}"

      - name: Verify release asset exists
        run: |
          echo "🔍 验证下载链接: ${{ steps.meta.outputs.download_url }}"
          HTTP_CODE=$(curl -sL -o /dev/null -w "%{http_code}" "${{ steps.meta.outputs.download_url }}")
          if [ "$HTTP_CODE" -ge 200 ] && [ "$HTTP_CODE" -lt 400 ]; then
            echo "✅ 下载链接有效 (HTTP $HTTP_CODE)"
          else
            echo "❌ 下载链接无效 (HTTP $HTTP_CODE)"
            echo "⚠️ Release 资产可能尚未上传完成，等待 30 秒后重试..."
            sleep 30
            HTTP_CODE=$(curl -sL -o /dev/null -w "%{http_code}" "${{ steps.meta.outputs.download_url }}")
            if [ "$HTTP_CODE" -ge 200 ] && [ "$HTTP_CODE" -lt 400 ]; then
              echo "✅ 重试成功 (HTTP $HTTP_CODE)"
            else
              echo "❌ 重试仍然失败 (HTTP $HTTP_CODE)，跳过索引更新"
              exit 1
            fi
          fi

      # ── Fork 索引仓库（如果尚未 fork） ──────────────────────────
      - name: Ensure fork exists
        id: fork
        env:
          GH_TOKEN: ${{ secrets.INDEX_PAT }}
        run: |
          # 获取 PAT 对应的 GitHub 用户名
          PAT_USER=$(curl -sS -H "Authorization: token ${GH_TOKEN}" \
            https://api.github.com/user | jq -r '.login')
          if [ -z "$PAT_USER" ] || [ "$PAT_USER" = "null" ]; then
            echo "❌ 无法获取 PAT 对应的 GitHub 用户名，请检查 INDEX_PAT 是否有效"
            exit 1
          fi
          echo "pat_user=$PAT_USER" >> $GITHUB_OUTPUT

          FORK_REPO="${PAT_USER}/${{ matrix.index.name }}"
          echo "fork_repo=$FORK_REPO" >> $GITHUB_OUTPUT
          echo "👤 PAT 用户: $PAT_USER"
          echo "🍴 Fork 仓库: $FORK_REPO"

          # 检查 fork 是否已存在
          HTTP_CODE=$(curl -sS -o /dev/null -w "%{http_code}" \
            -H "Authorization: token ${GH_TOKEN}" \
            "https://api.github.com/repos/${FORK_REPO}")

          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ Fork 已存在: $FORK_REPO"
          else
            echo "🍴 Fork 不存在，正在创建..."
            FORK_RESULT=$(curl -sS -X POST \
              -H "Authorization: token ${GH_TOKEN}" \
              -H "Accept: application/vnd.github+json" \
              "https://api.github.com/repos/${{ matrix.index.repo }}/forks" \
              -d '{"default_branch_only": true}')

            FORK_FULL_NAME=$(echo "$FORK_RESULT" | jq -r '.full_name')
            if [ -z "$FORK_FULL_NAME" ] || [ "$FORK_FULL_NAME" = "null" ]; then
              echo "❌ Fork 创建失败:"
              echo "$FORK_RESULT" | jq .
              exit 1
            fi
            echo "✅ Fork 创建成功: $FORK_FULL_NAME"

            # 等待 fork 完成初始化
            echo "⏳ 等待 fork 初始化..."
            sleep 10
          fi

      # ── 同步 fork 到上游最新 ───────────────────────────────────
      - name: Sync fork with upstream
        env:
          GH_TOKEN: ${{ secrets.INDEX_PAT }}
        run: |
          echo "🔄 同步 fork 到上游最新..."
          SYNC_RESULT=$(curl -sS -X POST \
            -H "Authorization: token ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ steps.fork.outputs.fork_repo }}/merge-upstream" \
            -d '{"branch": "${{ env.INDEX_BRANCH }}"}')
          echo "$SYNC_RESULT" | jq -r '.message // "已同步"'

      - name: Checkout index repo
        uses: actions/checkout@v4
        with:
          repository: ${{ matrix.index.repo }}
          ref: ${{ env.INDEX_BRANCH }}
          path: plugin-index
          token: ${{ secrets.INDEX_PAT }}

      - name: Update plugins.v4.json
        env:
          PLUGIN_ID: ${{ steps.meta.outputs.plugin_id }}
          PLUGIN_NAME: ${{ steps.meta.outputs.plugin_name }}
          PLUGIN_VERSION: ${{ steps.meta.outputs.version }}
          PLUGIN_DESCRIPTION: ${{ steps.meta.outputs.description }}
          PLUGIN_AUTHOR: ${{ steps.meta.outputs.author }}
          PLUGIN_HOMEPAGE: ${{ steps.meta.outputs.homepage }}
          PLUGIN_DOWNLOAD_URL: ${{ steps.meta.outputs.download_url }}
          PLUGIN_TAGS: ${{ steps.meta.outputs.tags }}
          PLUGIN_MIN_VERSION: ${{ steps.meta.outputs.min_version }}
          INDEX_FILE_NAME: ${{ env.INDEX_FILE }}
        run: |
          cd plugin-index

          node -e "
            const fs = require('fs');
            const data = JSON.parse(fs.readFileSync(process.env.INDEX_FILE_NAME, 'utf-8'));

            const pluginId = process.env.PLUGIN_ID;
            const newPlugin = {
              id: pluginId,
              name: process.env.PLUGIN_NAME,
              version: process.env.PLUGIN_VERSION,
              description: process.env.PLUGIN_DESCRIPTION,
              author: process.env.PLUGIN_AUTHOR,
              homepage: process.env.PLUGIN_HOMEPAGE,
              downloadUrl: process.env.PLUGIN_DOWNLOAD_URL,
              tags: JSON.parse(process.env.PLUGIN_TAGS),
              minVersion: process.env.PLUGIN_MIN_VERSION
            };

            // 查找是否已存在
            const idx = data.plugins.findIndex(p => p.id === pluginId);
            if (idx >= 0) {
              console.log('📝 更新已有插件: ' + pluginId);
              data.plugins[idx] = newPlugin;
            } else {
              console.log('➕ 新增插件: ' + pluginId);
              data.plugins.push(newPlugin);
            }

            fs.writeFileSync(process.env.INDEX_FILE_NAME, JSON.stringify(data, null, 2) + '\n');
            console.log('✅ plugins.v4.json 已更新');
          "

      # ── 通过 fork 提交 PR 到对应索引仓库 ─────────────────────────
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          path: plugin-index
          token: ${{ secrets.INDEX_PAT }}
          push-to-fork: ${{ steps.fork.outputs.fork_repo }}
          commit-message: "chore: update ${{ steps.meta.outputs.plugin_id }} to v${{ steps.meta.outputs.version }}"
          branch: "auto-update/${{ steps.meta.outputs.plugin_id }}-v${{ steps.meta.outputs.version }}"
          delete-branch: true
          title: "🔌 [${{ steps.meta.outputs.plugin_name }}] 更新至 v${{ steps.meta.outputs.version }}"
          body: |
            ## 🤖 自动更新插件索引

            | 字段 | 值 |
            |------|-----|
            | **目标索引** | `${{ matrix.index.repo }}`（${{ matrix.index.label }}） |
            | **插件 ID** | `${{ steps.meta.outputs.plugin_id }}` |
            | **插件名称** | ${{ steps.meta.outputs.plugin_name }} |
            | **版本** | v${{ steps.meta.outputs.version }} |
            | **作者** | ${{ steps.meta.outputs.author }} |
            | **下载链接** | ${{ steps.meta.outputs.download_url }} |
            | **主页** | ${{ steps.meta.outputs.homepage }} |
            | **最低版本** | ${{ steps.meta.outputs.min_version }} |

            ---

            > 此 PR 由 CI 自动创建，来源: [${{ github.repository }}](${{ steps.meta.outputs.homepage }}) Release ${{ steps.meta.outputs.tag_name }}
